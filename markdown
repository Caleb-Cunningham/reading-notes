Markdown Notes:

Markdown is pretty much a simplified HTML in terms of web setup
Markdown uses things like # and replaces old commands like <h1> <h2> <h3>
Markdown also allows you to make list both ordered and unordered by simple putting * infront for unordered and 1. for ordered
